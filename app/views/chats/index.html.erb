<h1> Welcome <%= current_user.name %>!</h1>
<%= link_to 'Log out', destroy_user_session_path, method: :delete %>
<h3> Chat list</h3>
<% @users.each do |user| %>
<div><%= link_to user.name, chat_path(user.id) %> [<%= @messages_count[user.id] %>]</div>
<% end %>
